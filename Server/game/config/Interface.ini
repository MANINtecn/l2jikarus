# ---------------------------------------------------------------------------
# Interface Settings
# ---------------------------------------------------------------------------

# Enable L2jMobius GUI when OS supports it.
# Provides access to admin commands without the need to be online.
# Default: True
# IKARUS: desligado - a GUI (AWT/Swing) crashava no Windows Server via RDP
# (awt.dll WGlobalCursorManager.getCursorPos). Headless = sem esse crash.
EnableGUI = False

# Dark theme.
# Use a dark version of the Nimbus theme.
# Default: True
DarkTheme = True
